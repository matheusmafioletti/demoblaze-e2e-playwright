name: Playwright Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:
    inputs:
      browser:
        description: 'Browser to run tests on'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - chromium
          - firefox
          - webkit
      include_mobile:
        description: 'Include mobile tests'
        required: true
        default: true
        type: boolean

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Download history from previous run
      if: always()
      continue-on-error: true
      uses: dawidd6/action-download-artifact@v3
      with:
        name: history
        path: allure-history-previous
        workflow: playwright.yml
        workflow_conclusion: success

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 18

    - name: Install dependencies
      run: npm ci

    - name: Install Playwright Browsers
      run: npx playwright install --with-deps
    
    - name: Prepare history before tests
      if: always()
      run: |
        echo "📜 Preparing Allure history before running tests..."
        
        if [ -d "allure-history-previous" ]; then
          echo "✅ Found history artifact from previous run"
          echo "📊 Copying history to allure-results..."
          mkdir -p allure-results/history
          cp -r allure-history-previous/* allure-results/history/
          echo "✅ History copied successfully"
          echo ""
          echo "📁 History contents:"
          ls -la allure-results/history/
          echo ""
          echo "✅ History is ready - trend widgets will be generated"
        else
          echo "⚠️ No history artifact found (normal for first run)"
          echo "   History will be created after this run"
          echo "   Trend widgets will appear starting from run #2"
        fi
    
    - name: Run Playwright tests
      run: |
        # Determine which projects to run
        if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
          # Manual trigger - use selected options
          PROJECTS=""
          
          # Add desktop browsers based on selection
          if [ "${{ inputs.browser }}" == "all" ]; then
            PROJECTS="--project=chromium --project=firefox --project=webkit"
          else
            PROJECTS="--project=${{ inputs.browser }}"
          fi
          
          # Add mobile if selected
          if [ "${{ inputs.include_mobile }}" == "true" ]; then
            PROJECTS="$PROJECTS --project=mobile-chrome --project=mobile-safari"
          fi
          
          echo "Running tests with projects: $PROJECTS"
          npx playwright test $PROJECTS
        else
          # Automatic trigger (push/PR) - run all tests
          echo "Running all tests (automatic trigger)"
          npm test
        fi

    - name: Generate Allure Report
      if: always()
      run: |
        echo "📊 Generating Allure Report..."
        
        # Check if history exists before generation
        if [ -d "allure-results/history" ]; then
          echo "✅ History found in allure-results - trends will be generated"
          ls -la allure-results/history/
        else
          echo "⚠️ No history in allure-results (first run)"
        fi
        
        echo ""
        echo "🔧 Running allure generate..."
        npx allure generate allure-results --clean -o allure-report
        
        echo ""
        echo "✅ Allure report generated successfully"
    
    - name: Verify Allure Report
      if: always()
      run: |
        echo "🔍 Verifying Allure report generation..."
        
        if [ -d "allure-report" ]; then
          echo "✅ allure-report directory created"
          echo ""
          echo "📁 Report structure:"
          ls -la allure-report/ | head -15
          
          if [ -d "allure-report/history" ]; then
            echo ""
            echo "📊 History directory found in report:"
            ls -la allure-report/history/
          else
            echo "⚠️ No history/ in report (normal for first run)"
          fi
          
          echo ""
          echo "📈 Checking widgets directory for trends:"
          if [ -d "allure-report/widgets" ]; then
            ls -la allure-report/widgets/
            echo ""
            echo "🔍 Looking for trend widgets:"
            ls allure-report/widgets/ | grep -i trend || echo "   ⚠️ No trend widgets found!"
            
            # Check if trend files exist
            TREND_COUNT=$(ls allure-report/widgets/*trend* 2>/dev/null | wc -l)
            if [ "$TREND_COUNT" -gt 0 ]; then
              echo "   ✅ Found $TREND_COUNT trend widget files"
            else
              echo "   ❌ No trend widgets generated!"
              echo "   This is why trends don't appear in the UI"
            fi
          else
            echo "❌ Widgets directory not found!"
          fi
        else
          echo "❌ allure-report directory not found!"
          exit 1
        fi
    
    - name: Upload Allure Report
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: allure-report
        path: allure-report/
        retention-days: 30
    
    - name: Check if history was generated
      if: always()
      run: |
        echo "🔍 Checking history generation..."
        if [ -d "allure-report/history" ] && [ "$(ls -A allure-report/history)" ]; then
          echo "✅ History directory exists and is not empty"
          echo "📊 Files in history:"
          ls -la allure-report/history/
          echo ""
          echo "📄 Checking history.json content:"
          if [ -f "allure-report/history/history.json" ]; then
            echo "File size: $(wc -c < allure-report/history/history.json) bytes"
            head -c 500 allure-report/history/history.json || true
          fi
        else
          echo "⚠️ History directory is empty or doesn't exist"
          echo "   This is normal for the first run"
          echo "   Trends will appear starting from the second run"
        fi
    
    - name: Upload history for next run
      uses: actions/upload-artifact@v4
      if: always() && hashFiles('allure-report/history/**') != ''
      with:
        name: history
        path: allure-report/history/
        retention-days: 90
        overwrite: true

  # Status check job for PR protection
  test-status:
    name: Test Status Check
    needs: test
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Check test results
        run: |
          if [ "${{ needs.test.result }}" == "success" ]; then
            echo "✅ All tests passed successfully!"
            echo "✅ PR can be merged"
            exit 0
          else
            echo "❌ Tests failed or were cancelled"
            echo "❌ PR cannot be merged until tests pass"
            exit 1
          fi

  deploy:
    needs: test
    if: always()
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Download Allure Report
        uses: actions/download-artifact@v4
        with:
          name: allure-report
          path: allure-report
      
      - name: Prepare deployment package
        run: |
          echo "📊 Preparing Allure Report for GitHub Pages..."
          
          # Verify allure-report exists
          if [ ! -d "allure-report" ] || [ ! "$(ls -A allure-report)" ]; then
            echo "❌ ERROR: allure-report not found or empty"
            exit 1
          fi
          
          echo "📁 Report root contents:"
          ls -la allure-report/
          
          echo ""
          echo "📂 Checking critical files for trends:"
          
          # Check widgets directory
          if [ -d "allure-report/widgets" ]; then
            echo "✅ widgets/ directory exists"
            echo "📂 All widgets:"
            ls -la allure-report/widgets/
            echo ""
            echo "🔍 Trend widget files:"
            ls allure-report/widgets/ | grep -E "trend" || echo "   ⚠️ No trend widgets found!"
          fi
          
          # Check history directory  
          if [ -d "allure-report/history" ]; then
            echo ""
            echo "✅ history/ directory exists"
            ls -la allure-report/history/
            
            echo ""
            echo "📊 Checking history.json:"
            if [ -f "allure-report/history/history.json" ]; then
              FILE_SIZE=$(wc -c < allure-report/history/history.json)
              echo "   File size: $FILE_SIZE bytes"
              if [ "$FILE_SIZE" -gt 100 ]; then
                echo "   ✅ History has data (trends should be visible)"
              else
                echo "   ⚠️ History file is too small (may not have enough data yet)"
              fi
            else
              echo "   ❌ history.json not found!"
            fi
          else
            echo ""
            echo "⚠️ No history/ directory in report"
            echo "   This means no trends will be visible yet"
            echo "   (Normal for first run - trends appear from run #2 onwards)"
          fi
          
          echo ""
          echo "✅ Report ready for deployment!"
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
        
      - name: Upload to Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'allure-report'
          
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        
      - name: Deployment Summary
        run: |
          echo "🎉 Allure Report deployed successfully!"
          echo "📊 View your report at: ${{ steps.deployment.outputs.page_url }}"
